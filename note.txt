các thư viện cần nhớ

đè phương thức:
method-override

gửi data từ fe -> be:
body-parser

hiển thị thông báo:  
npm i express-flash
npm i express-session
npm i cookie-parser


hiển thị slug
npm i mongoose-slug-updater 

tải ảnh lên
npm i multer

kết nối db online 
npm install mongodb

 khi kết nối chú ý đúng :Collection Name

mã hoá mật khẩu : MD5


render lại trang trước:
  res.redirect(req.get("Referrer") || "/");

thu vien chuyen doi date : npm i moment


thư viện gửi mã sang mail : Nodemailer